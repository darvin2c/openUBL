---
title: Catálogo 06 — Tipo de documento de identidad
description: Códigos SUNAT de tipos de documento de identidad para emisores y clientes en openUBL.
---

import CodeTabs from '../../../components/CodeTabs.astro';

Catálogo SUNAT que identifica el tipo de documento de identidad del emisor o del adquirente/usuario de un comprobante electrónico.

## Tabla de códigos

| Código | Descripción |
|--------|-------------|
| 0 | Documento no domiciliado |
| 1 | DNI |
| 4 | Carnet de extranjería |
| 6 | RUC |
| 7 | Pasaporte |

## Uso típico

Se utiliza en:

- `Cliente.tipoDocumentoIdentidad`
- `Proveedor.ruc` se asocia implícitamente al tipo `6` (RUC) en el XML.

SUNAT exige que el emisor siempre se identifique con RUC (`6`). El cliente puede usar cualquiera de los códigos del catálogo según su condición.

## Ejemplo

<CodeTabs tabs={[
  {
    id: 'python',
    label: 'Python',
    lang: 'python',
    code: `from openubl.models import Invoice, Proveedor, Cliente, DocumentoVentaDetalle
from decimal import Decimal

invoice = Invoice(
    serie="F001",
    numero=126,
    proveedor=Proveedor(
        ruc="20100100100",  # Siempre RUC (tipo 6)
        razonSocial="Mi Empresa S.A.C.",
    ),
    cliente=Cliente(
        nombre="Juan Pérez",
        numeroDocumentoIdentidad="12345678",
        tipoDocumentoIdentidad="1",  # DNI
    ),
    detalles=[
        DocumentoVentaDetalle(
            descripcion="Producto A",
            cantidad=Decimal("1"),
            precio=Decimal("80.00"),
            tipoAfectacionIGV="10",
        )
    ],
    moneda="PEN",
)`,
  },
  {
    id: 'typescript',
    label: 'TypeScript',
    lang: 'typescript',
    code: `import { client, zInvoice } from "@openubl/sdk";

const invoice = zInvoice.parse({
  serie: "F001",
  numero: 126,
  proveedor: {
    ruc: "20100100100",
    razonSocial: "Mi Empresa S.A.C.",
  },
  cliente: {
    nombre: "Juan Pérez",
    numeroDocumentoIdentidad: "12345678",
    tipoDocumentoIdentidad: "1",
  },
  detalles: [
    {
      descripcion: "Producto A",
      cantidad: 1,
      precio: 80.00,
      tipoAfectacionIGV: "10",
    },
  ],
  moneda: "PEN",
});

const { data, error } = await client.post("/api/v1/invoice/create", { body: invoice });
if (error) throw new Error(JSON.stringify(error));`,
  },
  {
    id: 'curl',
    label: 'cURL',
    lang: 'bash',
    code: `curl -X POST http://localhost:8000/api/v1/invoice/create \
  -H "Content-Type: application/json" \
  -d '{
    "serie": "F001",
    "numero": 126,
    "proveedor": { "ruc": "20100100100", "razonSocial": "Mi Empresa S.A.C." },
    "cliente": { "nombre": "Juan Pérez", "numeroDocumentoIdentidad": "12345678", "tipoDocumentoIdentidad": "1" },
    "detalles": [{ "descripcion": "Producto A", "cantidad": 1, "precio": 80.00, "tipoAfectacionIGV": "10" }],
    "moneda": "PEN"
  }'`,
  },
]} />
